let d: int = 2;

fun test(a: int, b: int) -> int {
	__print a;
	__print b;

	let some_bool: bool = a < b;

	let f: int = test(1, a);

	if (a < a) 	 {
		let c: int = a + b;
		return c;
	}
}

 let a: int = test(1, true);
