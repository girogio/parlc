let w: int = __width;

let h: int = __height;

for (let x: int = 0; x < w; x = x + 1) {
	for (let y: int = 0; y < h; y = y + 1) {
		let z: int = x + y;
		while (z >= 2)  {
			z = z - 2;
		}
		if (z == 0) {
			__write x, y, #769656;
		}
	}
}

